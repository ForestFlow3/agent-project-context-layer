# AGENT_BRIEF

## 中文

这是一个 synthetic 示例输出。

### 先读这个

这个 synthetic 项目使用 Agent Project Context Layer。

在规划或编辑前，读取 `PROJECT_CONTEXT.md`、`DECISIONS.md`、`RISKS.md`、`TODO.md`、Open Questions 和 Source Map。

### 当前目标

为 `Spark Shelf` 准备第一个 static demo：一个包含想法列表、tag 过滤、选中想法详情和大纲占位的灵感收集箱。

### 当前下一步行动

定义 v0.1 想法 data model，然后创建一个小型 UI wireframe。

### 已接受边界

- 静态 local web app。
- 不做 backend。
- 不做 login。
- 不接 external API。
- 不做 analytics。
- 不做 cloud sync。
- 不做完整 AI writing app 行为。

### 已知风险

- 字段太多可能让捕捉想法变慢。
- AI outline 输出可能编造细节。
- local browser storage 可能丢失。

### 交接摘要

`Spark Shelf` 处于原型规划阶段。只构建一个小型 local-first demo，保持范围克制，使用 synthetic test ideas，并在稳定变化后更新 context files。

## English

This is a synthetic example output.

### Read This First

This synthetic project uses Agent Project Context Layer.

Before planning or editing, read `PROJECT_CONTEXT.md`, `DECISIONS.md`, `RISKS.md`, `TODO.md`, Open Questions, and Source Map.

### Current Objective

Prepare the first static demo for `Spark Shelf`: an inspiration inbox with idea list, tag filter, selected idea detail, and outline placeholder.

### Current Next Action

Define the v0.1 idea data model, then create a small UI wireframe.

### Accepted Boundaries

- Static local web app.
- No backend.
- No login.
- No external API.
- No analytics.
- No cloud sync.
- No full AI writing app behavior.

### Known Risks

- Too many fields may slow capture.
- AI outline output may invent details.
- Local browser storage can be lost.

### Handoff Summary

`Spark Shelf` is at prototype planning stage. Build only a small local-first demo, keep scope tight, use synthetic test ideas, and update context files after durable changes.
